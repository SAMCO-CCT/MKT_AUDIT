# SAMCO Weekly Audit - Next.js Version

## Run

```bash
npm install
npm run dev
```

Open: http://localhost:3000/audit

This page posts to `/api/audit` when clicking `บันทึก + ส่ง Email`. Add the API route from the PostgreSQL/email example if it is not already in your project.